# PCS Obsidian adapter

The plugin only reads a selected PCS profile and creates a local preview note.
It does not submit imports, confirm values, or modify existing notes. Store the
client ID and token in Obsidian local storage for a local-only setup.
